import { cva } from "class-variance-authority";

export const notFoundPageContainerStyles = cva([
  "flex h-screen flex-col items-center justify-center px-6",
  "bg-[#242424] text-center font-sans text-my-tusky-peach",
]);
